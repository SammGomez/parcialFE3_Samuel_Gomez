import React from 'react'
import Form from '../Form/Form'
import { useState } from 'react'
import "./Card.css"

const Card = (/*{no se que prop poner :( )}*/) => {
    return (
      <div className="card">
        <p> aca queria renderizar los datos capturados del usuario, pero no logro hacerlo :c</p>
      </div>
    );
  };
  
  export default Card;